xgb_params:
  objective: binary:logistic
  eval_metric: logloss
  learning_rate: 0.05
  max_depth: 6
  subsample: 0.8
  colsample_bytree: 0.8
  min_child_weight: 1
  scale_pos_weight: 2.77
  seed: 42
  nthread: -1

training:
  num_boost_round: 500
  early_stopping_rounds: 20
